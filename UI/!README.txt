This is the directory where the files for the application UI reside
each function is exlained in the python file
migrations are log files
spoor directory contians training info
static refences static files that are needed in webpages
templates contains all the html files that are to be rendered